# ============================================
# Dynsense Environment — Copy to .env.production
# All values have working defaults for localhost
# ============================================

# PostgreSQL
POSTGRES_USER=dynsense
POSTGRES_PASSWORD=dynsense_prod
POSTGRES_DB=dynsense

# Redis
REDIS_PASSWORD=dynsense_redis

# JWT (generate a real one for production: openssl rand -hex 64)
JWT_SECRET=dynsense-local-dev-secret

# Optional: Claude AI
ANTHROPIC_API_KEY=
